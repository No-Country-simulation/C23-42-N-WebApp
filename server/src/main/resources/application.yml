spring:
  application:
    name: server

  #db configuration data
  datasource:
    url: jdbc:mysql://localhost/musync
    username: root

  jpa:
    properties:
      hibernate:
        #FOR TESTING ONLY!!!
        format_sql: true
    #FOR TESTING ONLY!!!
    show-sql: true

  data:
    web:
      pageable:
        page-parameter: pagina
        size-parameter: tamano
    sort:
      sort-parameter: orden

server:
  error:
    include-stacktrace: never


#  mvc:
#    servlet:
#      path: /api/v1/demo
